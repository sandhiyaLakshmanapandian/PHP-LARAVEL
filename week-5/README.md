Tasks

Integrate bootstrap template for blog’s CRUD UI - https://startbootstrap.com/themes - The link is for reference purposes only, You should choose a free template from a website and confirm it with your mentors

Use Bootstrap template, Don’t use CDN or any other alternatives

Create blog CRUD (create, read, update, Delete)

File Upload must be used for user profile or blog image uploads.

Handle Validation for Inputs

Add Authentication

Do not use other Laravel UI concepts for authentication, only use the default Laravel UI (laravel/ui) authentication

Store the session token in the database.

Store the count of blogs of the authenticated user in session and use them to show the count in sidebar

