Tasks:

Create a new Laravel 12 project.

Recreate your previous Week 3 Student Marks Entry form inside the Laravel application (don’t reuse old PHP code).

Set up the database and tables using Laravel migrations.

Do not use your previous database connection or schema; create new migrations.

Add CSRF protection for all forms.

Create model factories for all models and seed data into the tables using factories and seeders.

Implement Soft Delete for one or more models (e.g., Student).

Use Query Builder for any one route, and use Eloquent queries for all other routes.

Display the data (with soft-deleted record filtering) in a Blade view.

